---
- hosts: copelco_docker
  become: yes
  tasks:
  - apt:
      update_cache: yes
  - import_role:
      name: geerlingguy.github-users
    tags: [users]
  - import_role:
      name: geerlingguy.security
    tags: [security]
  - import_role:
      name: geerlingguy.docker
    tags: [docker]
